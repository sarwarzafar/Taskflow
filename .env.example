# Copy this to .env for local (non-Docker) runs if you want to override defaults.
# docker-compose.yml sets REDIS_URL itself, so you don't need this file for Docker.

REDIS_URL=redis://localhost:6379/0
